name: the-grove-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - numpy=1.*
  - pandas
  - matplotlib
  - scikit-learn
  - tqdm
  - pip:
    - bpy

variables:
  PYTHONPATH: $CONDA_PREFIX/../../src/the_grove_22/modules:$PYTHONPATH
